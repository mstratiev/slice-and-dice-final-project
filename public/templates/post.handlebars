<div class="container-fluid color-mid">
    <div class="row row-fixed"><div class="row text-left">
            <div class="trail">
                <a href="#/home">Home</a><span>&gt&gt</span>
                <a href="#/posts">Blog</a><span>&gt&gt</span>
                <a href="#/posts/{{post.id}}">Current post</a>
            </div>
        </div>
        <div class="col-md-7">
            <div class="row text-left">
            <h2 class="titles">{{post.title}}</h2>
            </div>
            {{!-- Post --}}
            <div class="container-fluid current-post">
                <div class="row">
                    Posted by {{post.user.username}} on {{post.dateConverted}} in {{post.category}} | {{post.commentsCount}} Comments
                </div>
                <div class="row">
                    <div class="col-md-4">
                        <img src="{{post.image}}" alt="">
                    </div>
                    <div class="col-md-8">
                        {{post.body}}
                    </div>
                </div>
            </div>
            {{!-- Post comments --}}
            <div class="container-fluid comments">
                <h2>{{post.commentsCount}} Comments</h2> {{#each post.comments}}
                <div class="container-fluid">
                    <div class="row">
                        <div class="col-md-1">
                            <div class="row">
                                <img src="{{this.user.image}}" alt="" />
                            </div>
                            <div class="row">
                                <span class="date">{{this.date}}</span>
                            </div>
                        </div>
                        <div class="col-md-11">
                            <h4>{{this.user.username}} says:</h4>
                            <div class="container-fluid">
                                {{this.body}}
                            </div>
                            <div class="container-fluid">
                                <button class="btn btn-default btn-xs pull-right">Read more</button>
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}
                <div class="container-fluid">
                	<h3 class="leave a comment">Leave a Comment</h3>
                	{{!-- Insert comment form --}}
                </div>
            </div>
        </div>
        {{!-- Recent things --}}
        <div class="col-md-4 recent">
            <div class="container-fluid posts text-left">
                <h4 class="text-left">Recent Posts</h4>
                <div class="container-fluid color-light field">
                    <ul>
                        {{#each recentPosts}}
                        <li><a href="#/posts/{{id}}">{{title}}</a></li>
                        {{/each}}
                    </ul>
                </div>
            </div>
            <div class="container-fluid comments text-left">
                <h4 class="text-left">Recent Comments</h4>
                <div class="container-fluid color-light field">
                    <ul>
                        {{#each recentComments}}
                        <li><a href="#/posts/{{postId}}">{{body}}</a></li>
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
